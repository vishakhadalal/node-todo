{
  "name": "node-todo",
  "version": "0.0.1",
  "description": "Simple Node.js todo app",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist/",
    "start": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
